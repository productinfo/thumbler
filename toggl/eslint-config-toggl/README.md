# eslint-config-toggl

Reusable ESLit configuration.

## Usage

In `package.json`, specify:

```
"eslintConfig": {
  "extends": "toggl"
},
"dependencies": {
  "eslint-config-toggl": "1.0.0-next"
}
```
